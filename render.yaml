# PostgreSQL Database
databases:
  - name: bookingexperts-db
    databaseName: bookingexperts_production
    user: bookingexperts
    plan: free 
    region: frankfurt 

# Rails Web Application
services:
  - type: web
    name: bookingexperts-web
    env: ruby
    region: frankfurt 
    plan: free 
    buildCommand: "./bin/render-build.sh"
    startCommand: "./bin/render-start.sh"
    healthCheckPath: /up
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: bookingexperts-db
          property: connectionString
      - key: RAILS_ENV
        value: production
      - key: RAILS_MASTER_KEY
        sync: false 
      - key: RAILS_LOG_TO_STDOUT
        value: enabled
      - key: RAILS_SERVE_STATIC_FILES
        value: enabled

